// top-level exec; boolean type demo
bool myBool = true;
myBool = transformBool(myBool);
printBool(myBool);

fn printBool(bool val) {
    print(val as str);
}

fn transformBool(bool val) -> bool {
    return (val && false || true) && true != false; 
}
